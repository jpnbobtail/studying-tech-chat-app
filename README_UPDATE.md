# ローカルでの確認手順（今回の変更を試す）

## 前提
- Node 18+ / pnpm or npm / Next.js アプリ
- .env に Supabase と NEXT_PUBLIC_SUPABASE_* を設定
- Prisma を使用している場合は DB マイグレーション済み

## インストール
```bash
# 依存インストール
pnpm install
# or
npm install
